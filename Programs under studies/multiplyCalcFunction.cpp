#include<iostream>
using namespace std;

#define newline '\n'

//int multiplyFunction(int,int);

int main ()
{
	
	int numArr [1];
	
	for (int idx = 0; idx <= sizeof(numArr); idx++){
		cout << "insert the values >>> " << newline;
		cin >> numArr[idx];	
	}
	//cout << multiplyFunction(numArr,2);
	/*int numVarOne;
	int numVarTwo;

	cout << "insert the first value >>> " << newline;
	cin >> numVarOne;
	cout << "insert the second value >>> " << newline;
	cin >> numVarTwo;*/
	
	
}
/*int multiplyFunction(int numbers,int size)
{
	int calcResult = numOne * numTwo;

	return calcResult;
}
/*int multiplyFunction(int numOne,int numTwo)
{

	int calcResult = numOne * numTwo;

	return calcResult;
}*/

