#include<iostream>
using namespace std;

int main()
{
	float x = 1. / 2. + 2. / 4;
	cout << x;
	/*float x = 1.0 / 4.0;
	int i = x;
	
	cout  << i;
	int  var = X10;
	cout << var;
	int i = 3, j, k;
	
	if (i > 0) j =2 + i * i;
	if (i <= 0) j = 2* i - 1;
	if (j >= 0) k = j % i + 2;
	if (j < 0) k = i % j + 2;
	if (k < 0) k = k % i % j;
	if (k >= 0) k = j % i % k;
	
	cout << k;
	
	
	
	int i = 3, j = ++i, k = ++i;
	
	cout << k << j <<i;
	int var = 010;
	 cout << var; 
	int i;
	
	cin >> i;
	cout << i << hex << i + i << oct << i;
	
	
	int i = 1;
	int j = ++i;
	int k = j++;
	
	cout << k;
	
	int i = 3, j = ++i, k = ++i;
	
	cin >> i;
	cout << k + i << j - i << i * i;
	
	float var = -1e-1;
	
	cout << var;
	
	int i = 3, j = 2, k = -1;
	
	if(i > 0){
		if(j <= 0){
			if (k < 0)
				k++;
			if (k <= 0)
			k--;
		}
		if (j  > 0)
		i++;
	}
	
	if (i <= 0)
	j++;
	
	k = i + j + k;
	
	cout << k;
	
	float x;
	auto
	cin >> x;
	cout << scientific << "x";
	int var = 0x1A;
	int k = 1 % 2 + 4 % 2;
	cout << k;*/

	return 0;
}